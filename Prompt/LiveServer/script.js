// Pede o nome do usuário usando uma caixa de ptompt
// O que o usuário digitar será salvo na variável "nome"
let nome = prompt("Digite seu nome:");

// Cria uma mensagem de boas-vindas
let mensagem = "Olá, " + nome + "! Seja bem-vindo!";

// Mostra a mensagem na tela usando uma caixinha de alerta
alert(mensagem);